{% extends "::admin.html.twig" %}

{% block title %}Ajout d'un nouvel arrêt au réseau - {{ parent() }}{% endblock %}

{% block body %}

<h1>Ajout d'un nouvel arrêt au réseau</h1>

<form action="{{ path('admin_arrets_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_arrets') }}">
            Back to the list
        </a>
    </li>
</ul>
	
{% endblock %}
